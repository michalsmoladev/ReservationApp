# Allow company owners to administer multiple companies

ReservationApp allows one tenant to administer multiple companies. This keeps ownership and administrator access centered on the authenticated owner instead of forcing a separate owner account per company, while requiring authorization checks to always include the selected company context.
